#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
<<<<<<< HEAD
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../eslint-plugin-react/node_modules/resolve/bin/resolve" "$@"
  ret=$?
else 
  node  "$basedir/../eslint-plugin-react/node_modules/resolve/bin/resolve" "$@"
  ret=$?
fi
exit $ret
=======
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../resolve/bin/resolve" "$@"
else 
  exec node  "$basedir/../resolve/bin/resolve" "$@"
fi
>>>>>>> b66879a1e341f4419aa469998bb6947e10c14fff
